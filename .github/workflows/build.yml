name: Build Android Kernel
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup build tools
        run: |
          sudo apt update
          sudo apt install -y gcc-aarch64-linux-gnu flex bison bc libssl-dev libelf-dev
          
      - name: Clone LineageOS kernel
        run: |
          git clone --depth 1 https://github.com/LineageOS/android_kernel_qcom_sm8550.git kernel
          
      - name: Copy DJI device tree and config
        run: |
          cp kalama_hdk.dts kernel/arch/arm64/boot/dts/qcom/
          cp config kernel/arch/arm64/configs/dji_defconfig
          
      - name: Build kernel
        run: |
          cd kernel
          export CROSS_COMPILE=aarch64-linux-gnu-
          export ARCH=arm64
          cp ../config .config
          scripts/config --disable WERROR
          scripts/config --disable DEBUG_INFO_BTF
          scripts/config --enable USER_NS
          # 不要 make olddefconfig，直接回答所有新配置选项为默认
          yes "" | make -j$(nproc) Image.gz
          
      - name: Upload Image
        uses: actions/upload-artifact@v4
        with:
          name: kernel-Image
          path: kernel/arch/arm64/boot/Image.gz
